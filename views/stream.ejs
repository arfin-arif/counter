<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <title>Stream</title>
    <style>
      .result-parent-div {
        display: flex;
        font-size: 100px;
        margin-top: 8rem;
      }

      .result-div-one,
      .result-div-two {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-items: center;
        padding: 100px;
        background-color: black;
        color: white;
        border-radius: 10px;
        margin: 20px;
      }
    </style>
  </head>
  <body>
    <div class="result-parent-div">
      <div class="result-div-one">
        <div id="countOne"><%= countOne %></div>
      </div>
      <div class="result-div-two">
        <div id="countTwo"><%= countTwo %></div>
      </div>
    </div>

    <!-- Include Socket.IO -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/3.0.3/socket.io.js"></script>
    <script>
      window.baseurl = "<%= baseurl %>";
    </script>
    <!-- Your script for handling increments and decrements -->
    <script src="/client.js"></script>
  </body>
</html>
